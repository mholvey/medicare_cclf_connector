version: 2

models:
    - name: eligibility
      description: Member enrollment and demographic detail
      tests:
         - unique:
             column_name: "(patient_id || '-' || payer || '-' || month || '-' || year)"