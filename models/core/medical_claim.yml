version: 2

models:
    - name: medical_claim
      description: Claim line level medical data
      tests:
         - unique:
             column_name: "(claim_id || '-' || claim_line_number)"