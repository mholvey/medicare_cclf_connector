version: 2

seeds:
  - name: medicare_state_fips
    config:
      database:  "{{ var('output_database') }}"
      schema:  "{{ var('terminology_schema')}}"
      tags: terminology
      column_types:
        fips_code: string